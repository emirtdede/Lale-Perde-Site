import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Lale Perde • Garanti Taahhütnamesi',
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
